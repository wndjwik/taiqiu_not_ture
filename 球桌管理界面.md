2.3 球桌管理模块
2.3.1 模块核心定位
实现桌位布局可视化自定义、球桌状态实时监控、开台/结台操作及预订管理，关联会员消费形成业务闭环，桌位使用数据同步至主页报表，支撑运营决策。
2.3.2 桌位布局可视化设计（管理员端）
2.3.2.1 界面布局
采用“背景网格+组件拖拽+操作面板”结构，兼顾可视化直观性与操作便捷性，与会员列表页为同级导航菜单：
- 顶部控制区：包含“布局编辑/预览切换”按钮、组件库（仅含球桌组件，收银台默认唯一）、网格吸附开关（默认开启）；
- 核心编辑区：灰色网格背景（50×50px标准网格），展示收银台与球桌组件，支持拖拽调整位置；
- 右侧属性面板：选中组件时显示对应属性，球桌组件可修改桌号、小时价、尺寸，收银台仅支持修改位置；
- 底部操作区：“保存布局”“恢复默认”“撤销”“重做”按钮，操作后实时生效。
2.3.2.2 核心交互规则
- 组件限制：收银台默认1个，初始化位于左下角，支持拖拽不可删除/旋转；球桌支持新增（最多20张）、删除，新增时默认生成连续桌号；
- 拖拽反馈：拖动组件时，原组件变为50%透明度，目标位置显示实色虚影（与组件同尺寸、同样式，颜色稍浅），松开后组件吸附至最近网格点；
- 球桌旋转：选中球桌后点击旋转按钮（↺），支持0°/90°/180°/270°四档切换，桌号文字始终保持水平正向（通过反向补偿旋转实现）；
- 边界约束：组件拖拽时不可超出编辑区边界，确保完整显示；
- 操作回溯：支持最近10步操作的撤销/重做，涵盖新增/删除球桌、拖拽调整、旋转、属性修改等场景。
2.3.2.3 布局数据存储
保存布局时，系统自动将布局信息序列化存储，数据结构关联数据库表，支持二次编辑调用：
{
  "layout_id": 1,
  "cashier_info": {
    "position": { "x": 50, "y": 50 },
    "size": { "width": 120, "height": 80 }
  },
  "tables_info": [
    {
      "table_id": 1,
      "table_no": "1号桌",
      "position": { "x": 200, "y": 150 },
      "size": { "width": 100, "height": 200 },
      "rotation": 0,
      "price_per_hour": 60
    }
  ],
  "updated_time": "2025-11-04 16:30:00"
}
2.3.3 球桌运营管理界面设计
2.3.3.1 界面布局
采用“状态筛选+可视化桌位+明细列表”三层结构，适配前台日常运营场景：
- 顶部筛选区：包含状态筛选（全部/空闲/使用中/已预订，默认“全部”）、桌号搜索（支持模糊查询，如输入“8”匹配“8号桌”）、营业时段选择（默认当日）；
- 中间可视化区：按保存的布局渲染桌位，通过颜色区分状态，-hover显示简要信息：
- 绿色：空闲（显示“点击开台”提示）；
- 红色：使用中（显示桌号、开台时间、当前使用人）；
- 黄色：已预订（显示桌号、预订时段、联系人）；
- 右侧明细列表区：展示当前状态为“使用中”“已预订”的桌位明细，字段包括桌号、状态、使用人/预订人、开始时间、预计结束时间、操作按钮。
2.3.3.2 核心业务流程
2.3.3.2.1 开台流程（关联会员消费）
1. 选中空闲球桌，点击“开台”按钮，弹出开台弹窗；
2. 会员关联：支持搜索会员姓名/手机号快速关联，或选择“非会员”（单独记录），关联后显示会员余额；
3. 计费设置：默认加载球桌预设小时价，支持管理员权限临时修改，可选择“按小时计费”或“包时计费”（如2小时100元）；
4. 确认开台：系统校验信息后，球桌状态变为“使用中”，开始计时并生成临时开台记录，同步展示至可视化区与明细列表。
2.3.3.2.2 结台流程（自动生成消费）
1. 选中使用中球桌，点击“结台”按钮，弹出结台弹窗；
2. 费用计算：系统自动计算使用时长（不足1小时按1小时计，支持手动调整）及总费用，展示明细（时长×单价）；
3. 支付结算：支持“会员卡扣款”“现金”“微信/支付宝”三种方式，会员卡扣款需验证会员密码，非会员直接确认支付方式；
4. 确认结台：系统扣除对应费用（会员卡扣款同步更新余额），生成消费记录，球桌状态恢复“空闲”，数据同步至主页报表，支持打印结台小票。
2.3.3.2.3 预订流程
1. 选中空闲球桌，点击“预订”按钮，弹出预订弹窗；
2. 填写信息：输入联系人姓名、手机号、预订时段（开始时间≥当前时间，时长≤8小时）；
3. 确认预订：系统校验时段冲突后，球桌状态变为“已预订”，生成预订记录；预订时段前30分钟，系统弹窗提醒前台确认到店；超时30分钟未到店且未联系，自动取消预订并恢复“空闲”状态。